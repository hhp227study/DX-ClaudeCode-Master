# 레퍼런스 댄스 영상 분석 → 채보 안무 규칙 (2026-07-08)

`reference/` 폴더의 셀카 댄스 영상들을 MediaPipe Pose(Landmarker Lite)로 전 프레임 분석해,
"자연스러운 셀카 댄스 동작"을 채보 생성 규칙으로 번역했다. 결과는 `web/scripts/gen-chart.mjs`의
안무(choreography) 상수에 반영되어 있다.

**표준 영상**: 유저가 `reference/best.mp4`를 "채보가 나타나야 하는 모습의 베스트 표준"으로
지정했다. 5개 영상 풀링은 공통 패턴(앵커존·교대·모티프)의 근거로, best.mp4는 y 높이와
모티프 가중치의 기준으로 쓴다.

## 분석 방법

- 영상: 세로 셀카 댄스 5개 (11~24초, 480~720p) — 클로즈업 3개, 상반신~전신 2개
- 파이프라인: OpenCV 프레임 디코드 → PoseLandmarker(VIDEO 모드) → 손목·어깨·코 궤적 추출
- 좌표는 어깨 중심·어깨 너비로 정규화(body unit)해 프레이밍 차이를 제거
- "드웰(dwell)" = 손목 속도가 낮게 유지되는 0.12초 이상 구간 = 동작이 비트에 걸리는 지점
  (노트가 놓여야 할 위치·간격의 근거)

## 핵심 수치 (5개 영상 풀링)

| 항목 | 값 | 채보 번역 |
|---|---|---|
| 드웰 y (화면 비율) | 중앙값 0.66, IQR 0.59~0.71, 90th 0.735 | 기본 노트는 밴드 하부(가슴 높이) 0.56~0.73에 집중. 상부 0.45~0.53은 액센트 전용 |
| 드웰 x | 좌우로 갈라진 쌍봉 (0.12/0.33 ↔ 0.52/0.79) | 좌/우 앵커존 x≈0.28 / 0.72 + 중앙존 0.5 |
| 같은 손 연속 이동 거리 | 중앙값 0.28bu, 90th 0.61bu (화면 \|dx\| 90th 0.27) | 같은 쪽 연속 노트는 랜덤 워크(스텝 상한)로 — 순간이동 배치 금지 |
| 손별 드웰 간격 | 중앙값 1.06초, IQR 0.73~1.5초 | 좌우 교대 배치 기준 노트 간격 0.75~1.5초가 자연 케이던스와 정합 → 비트 단위 간격(하한 0.75초)의 근거 |
| 좌우 손 속도 상관 | +0.14~+0.80 (전부 양수) | 손은 '대칭/미러'로 함께 움직임 → 프레이즈를 좌우 미러 구조로 |
| 손목이 코 위로 | ≤8% (짧은 액센트뿐) | **이 영상들의** 손은 얼굴 위로 거의 안 간다 → 이 곡들의 밴드는 0.45~0.73 유지. 2026-07-22 전까지는 이게 전역 금지 규칙이었다 |
| 손목이 어깨 위로 | 영상별 0~54% | 프레이즈 끝 y 0.45~0.53 액센트 + hands_up 포즈 노트로 반영 |
| 양손 근접(<0.45bu) | 시간의 0~20% | 중앙 모아주기 모티프(하트/박수 순간) + heart 포즈 노트 |
| 동작 주기(자기상관) | 0.23~1.83초 (전형 0.5~0.7초) | 실제 댄스 케이던스가 1.5초보다 빠름 → 비트 단위 간격 전환(2026-07-08)을 뒷받침 |

## best.mp4 (표준 영상) 추가 분석

15.2초, 720×1280, 단독 셀카 (어깨선 y≈0.55, 어깨너비 0.70 — 상반신 클로즈업 프레이밍).

| 항목 | 값 | 5영상 풀링과의 차이 → 반영 |
|---|---|---|
| 드웰 y | 중앙값 0.552, IQR 0.522~0.59, 90th 0.603 | **어깨선 높이에 집중** (풀링 0.59~0.71보다 위) → 기본 y 밴드를 0.56~0.73에서 **0.50~0.66**으로 상향 |
| 같은 손 이동 \|dx\| | 중앙값 0.071, 90th 0.121 | 제자리 반복 포인팅 — 더 타이트 → y 스텝 상한 0.12→**0.10**, x 지터 ±0.07 유지(90th와 일치) |
| 중앙 하트 액센트 | 1.4 / 6.2 / 10.0 / 14.7초 — **~4초 주기로 반복** | 이 영상의 시그니처 → heart 모티프를 easy에도 추가, 가중치 상향 (easy 25%, normal 30%) |
| 드웰 x | 쌍봉 0.23~0.36 / 0.80~0.87 (±0.3~0.57bu) | 풀링과 일치 → 앵커존 유지 |
| 드웰 간격 | 중앙값 1.1초 (하트 유지 시 2.6~3초) | 풀링과 일치 — 비트 단위 간격의 클라이맥스(0.86~1.25초)와 자연 케이던스가 맞닿음 |
| 손목이 코 위로 | 0% | 표준 영상 기준으로는 얼굴 위 노트가 불필요 (곡별 밴드로 분리 — 카라멜단센은 반대) |

## 도출된 안무 규칙 (gen-chart.mjs 구현)

1. **좌우 교대 그루브** — 노트는 좌(x≈0.28)/우(x≈0.72) 앵커존을 교대로 오간다.
   양손이 번갈아 움직이는 자연스러운 댄스가 되고, 동시에 화면에 떠 있는 두 노트가
   겹치지 않는다(가독성).
2. **4노트 프레이즈 모티프 (가중치 선택)** — 무작위 개별 배치 대신 4노트 단위 모티프를
   굴린다: `sway`(좌우 스웨이) / `arc`(팔 스윕: 바깥→안→안→바깥) / `build`(고조,
   끝은 액센트) / `heart`(좌·우·중앙·중앙 모아주기 — best.mp4 시그니처).
   가중치: easy sway45/arc30/heart25, normal sway30/arc20/build20/heart30.
3. **어깨~가슴 높이 우선 (best.mp4 표준)** — 기본 y는 0.50~0.66의 사이드별 랜덤 워크
   (경계 반사), 프레이즈 마지막 노트만 0.45~0.53 액센트.
   ⚠️ 이는 **이 표준 영상 계열 곡의 기본값**이고, 밴드 자체는 0.08~0.73까지 열려 있다
   (2026-07-22 '얼굴 가림 금지' 상한 폐지). 머리 위로 손을 올리는 곡은
   songs.json의 choreo.yBase·yAccent로 밴드를 올려 쓴다 — 카라멜단센 참고
4. **스텝 상한** — 같은 쪽 연속 노트의 이동 폭 제한(x 지터 ±0.07, y 스텝 ≤0.10).
   손이 관성적으로 흐르는 경로 위에 노트가 놓인다.
5. **함정(decoy)은 흐름의 반대편** — 직전에 잡은 자리 근처(잡은 손이 물러나는 자리)에
   y를 ±0.15 비켜 배치 — 주로 동선 아래 허리 높이(~0.73까지 허용)로 내려간다.
   다음 캐치와 겹치면 치환을 포기한다(억울한 함정 방지). 프레이즈 액센트 자리에는 두지 않는다.
6. **포즈 노트 유지** — hands_up/heart는 섹션 경계(고조 지점)에 그대로. 영상에서도
   손 올리기·하트는 프레이즈 클라이맥스에서만 짧게 나온다.

## 카라멜단센 영상 분석 (2026-07-12 추가 — 곡 'caramelldansen'의 근거)

`reference/carameldansen01.mp4`(26.9s)·`carameldansen02.mp4`(25.1s)·`carameldansen03.mp4`(26.9s,
스튜디오 트라이포드 풀바디·같은 날 추가), 720×1280 30fps. 서로 다른 댄서지만 같은 오디오
구간(Speedycake Remix 후렴)이라 교차 검증이 가능했다 — 멜로디 추출 결과가 8분음표 슬롯 단위로
3영상 98% 일치. 01/02에서 잘려 창작으로 메꿨던 마지막 마디(턴어라운드)는 03 덕에 실제 곡의
하강 해결(D#5 G#4 D#5 A#4 G#4 G#4 F#4 F#4)로 교정했다.

03 추가 관찰: 양손 y 상관 +0.98(완벽한 유니즌 바운스), 드웰 IOI ≈ 3비트(안무 히트가 명확),
**프레이즈 절정의 Y자 팔벌리기·머리 위 만세** → vspread 모티프(마지막 두 노트가 와이드 x 0.16/0.84
+ 상단 액센트)로 번역해 caramelldansen 채보에 가중치 15~20%로 반영.

| 항목 | 값 (01 / 02) | 채보·사운드 번역 |
|---|---|---|
| 오디오 템포 (비트그리드 정합) | 164.75 ≈ **165 BPM** (양쪽 동일) | 곡 BPM 165 확정 |
| 코드 진행 (마디별 크로마) | **F#–C#–D#m–B** (I–V–vi–IV, F# 장조) 4마디 루프 | 신스 커버 bass 진행. 멜로디는 후렴 8마디+오아오아 8마디 = 16마디 루프(synth.loopBars 16) |
| 손목 y (화면 비율) | 중앙값 0.377 / 0.332, IQR 0.26~0.44 / 0.25~0.40 | **화면 상단** — 기존 5영상 풀링(중앙값 0.66)과 정반대. "손 올리고 추는 춤" |
| 어깨 위 체류 | 양손 동시 42% / 59%, 한 손 이상 53% / 81% | **choreo.yBase [0.30, 0.48]** = 눈~어깨 높이 (구 밴드 0.45~0.73에서는 상부로만 근사할 수 있어 [0.46,0.58]이었다) |
| 코 위 체류 | 37% / 46% | 기존 영상들(≤8%)과 달리 상시 — **2026-07-22 밴드 상한 폐지로 노트로 직접 표현**(yAccent [0.10,0.24] = 정수리 위). 그전까지는 얼굴 가림 금지 규칙에 막혀 hands_up 포즈 노트로만 우회했다 |
| 강아지귀 존 (머리 옆 x·어깨 위) | L 30/34%, R 29/24% | 좌우 대칭 유지 — 앵커존(x 0.28/0.72) 그대로 |
| 양손 상관 | y +0.92 / +0.88 (함께 바운스), x -0.48 / -0.71 (미러) | 미러 프레이즈 구조 유지 |
| 스웨이 주기 (손목 x 자기상관) | 0.73~0.83초 ≈ **2비트** | 좌우 교대가 시그니처 → sway 가중치 상향 (2026-07-12 난이도 개편 후 easy 40, normal 35) |
| 드웰 | 손별 2~6개뿐 | 멈추지 않는 연속 바운스 — 드웰 기반이던 기존 분석과 질적으로 다른 춤이라 곡별 오버라이드(songs.json choreo)로 반영 |

## 실제 음원 도입 — Caramella Girls 원곡 분석 (2026-07-22)

신스 커버를 원곡 MP3(`Caramella Girls - Caramelldansen HD Version (Swedish Original)`)로 교체하면서
음원 자체를 분석해 비트 그리드를 확정했다. 분석 파이프라인은 miniaudio 디코드 → 스펙트럴 플럭스
온셋 포락선(1024/256 STFT) → 콤 필터 템포 추정 → 클릭 트레인 위상 탐색.

| 항목 | 측정값 | 채보 반영 |
|---|---|---|
| 원곡 길이 | 175.26초 (2:55), 44.1kHz 320kbps CBR | **전곡 수록** (175.2초) |
| 템포 | **164.73 BPM** | `songs.json` bpm — 영상 분석값(164.75)과 독립적으로 일치 |
| 첫 다운비트 | **162.5 ms** | `audio.firstBeatMs` = 비트 그리드 원점. 채보 `offsetMs`에도 기록 |
| 마디 길이 | 1.457초 (120마디) | 구간 경계를 마디에 맞춤 |
| 구조 (4마디 프레이즈 온셋 밀도) | 아래 표 참고 | `songs.json sections` 10개로 명시 배치 |

**BPM 정밀도가 중요한 이유**: 165로 반올림하면 87초 구간에서 비트 그리드가 약 142ms 어긋난다
(판정 윈도우 ±250ms의 절반이 넘는다). 그래서 카탈로그·채보는 164.73을 그대로 쓰고, 표시용으로만
반올림한다(DB `bpm` 컬럼은 int라 165 저장 — 판정에는 쓰이지 않는다).

**인코더 딜레이 검증**: MP3는 디코더에 따라 앞머리 무음 처리가 달라 채보 전체가 밀릴 수 있다.
Chrome headless에서 `decodeAudioData` 후 같은 방식으로 위상을 재측정해 **162.5ms 동일**,
길이 88.5638초 동일을 확인했다 (Python 분석과 어긋남 없음).

### 곡 구조 (4마디 프레이즈 단위 온셋 밀도, 마디 = 1.457초)

| 마디 | 시각 | 밀도 | 구간 | 채보 level |
|---|---|---|---|---|
| 0~11 | 0.2~17.6s | 0.16~0.35 | 인트로 | (6초부터) `sparse` |
| 12~19 | 17.6~29.3s | 0.52 | 빌드 | `low` |
| 20~31 | 29.3~46.8s | **0.74~0.77** | **후렴1** | `high` |
| 32~43 | 46.8~64.3s | 0.41~0.57 | 2절 | `low` |
| 44~51 | 64.3~75.9s | 0.53~0.56 | 프리코러스 | `mid` |
| 52~63 | 75.9~93.4s | **0.65~0.75** | **후렴2 (곡의 정점)** | `high` |
| 64~75 | 93.4~110.9s | 0.41~0.49 | 3절 | `low` |
| 76~83 | 110.9~122.5s | 0.45~0.53 | 프리코러스2 | `mid` |
| 84~91 | 122.5~134.2s | **0.20~0.25** | **브레이크다운** | `sparse` (휴식) |
| 92~115 | 134.2~169.2s | 0.41~0.54 | 마지막 후렴 | `mid` |
| 116~119 | 169.2~175.3s | 0.12 | 아웃트로(페이드) | 노트 없음 |

브레이크다운은 곡에서 유일하게 뚜렷한 쉼이라 채보에서도 휴식 구간으로 살렸다
(easy 2.19초 간격 4노트). 3분짜리 곡에서 팔을 계속 올리고 있어야 하는 게임 특성상
이 쉼이 체력 배분 지점이 된다. 포즈 노트는 구간 전환의 빈 자리에 배치한다
(47.5s 하트 / 94.9s 만세 / 133.1s 하트 — 마지막 후렴 직전에 기를 모으는 자리).

**전곡 vs 하이라이트**: 처음엔 하이라이트 87.6초만 프레임 단위로 잘라 수록했으나
(재인코딩 없이 7.01MB→3.55MB), "중간에 끊기니 어색하다"는 유저 피드백으로 전곡으로 되돌렸다.
전곡은 아웃트로가 자연 페이드라 인위적 페이드아웃(`audio.fadeOutMs`)이 필요 없다.
잘라 쓰는 방식이 다시 필요해지면 프레임 경계 컷 + Info(Xing) 헤더 patch로 비트 그리드가
보존됨은 검증해 뒀다 (잘린 파일 재측정 164.74 BPM / 162.5ms).

## 프레이밍 분류 — 클로즈업 셀카 vs 풀바디/트라이포드 (2026-07-12)

카라멜단센 영상들은 기존 레퍼런스(가까운 셀카)와 달리 전부 먼 풀바디 촬영이다.
MediaPipe 랜드마크 두 가지로 신뢰성 있게 구분된다 (실측):

| 영상 | 어깨너비 (화면 비율) | 어깨선 y | 골반 가시(vis>0.5) | 발목 가시 |
|---|---|---|---|---|
| best.mp4 (클로즈업 표준) | **0.70** | 0.55 | 0% | 0% |
| carameldansen01 | 0.23 | 0.37 | 100% | 96% |
| carameldansen02 | 0.23 | 0.37 | 100% | 91% |
| carameldansen03 | **0.19** | 0.32 | 100% | 100% |

**분류 규칙**: ① 발목 랜드마크(27/28) 가시성 ≥50% 프레임 → 풀바디/트라이포드,
② 어깨너비 ≥0.45 → 클로즈업(상반신), 0.25 미만 → 풀바디 (그 사이 = 상반신~전신).
두 지표는 실측에서 완전히 일치하며 ①이 더 강건하다 (어깨너비는 팔 동작에 따라 순간 변동).

**분석에 미치는 영향**: 화면 비율 좌표는 프레이밍에 따라 왜곡된다 (같은 "머리 옆 손"이
클로즈업에선 y 0.3, 풀바디에선 y 0.25에 찍히고 x 스프레드도 좁아진다). 그래서 이 문서의
파이프라인은 어깨 중심·어깨너비로 정규화한 body unit을 함께 쓰고, 게임 채보 번역 시에는
프레이밍별 통계를 곡별 choreo 오버라이드(yBase 등)로 환산한다. 게임 자체는 상반신 셀카
프레이밍이므로 풀바디 레퍼런스의 수치를 화면 좌표 그대로 옮기면 안 된다.

## 재현 방법

분석 스크립트는 세션 스크래치패드에서 실행했다(레포에 미포함 — 영상은 저작권상 커밋 금지).
재현: `pip install mediapipe opencv-python-headless numpy` 후 PoseLandmarker Lite로
손목(15,16)·어깨(11,12)·코(0) 랜드마크를 추출해 위 표의 통계를 계산하면 된다.

*확정 제품 규칙 중 y 밴드(0.45~0.73)·접근 2000ms는 유지. 간격 규칙은 2026-07-08 유저 요청으로
"최소 1.5초(BPM 무관)" → "비트 단위(BPM 비례, easy 6/5/4·normal 4/3/2비트, 하한 0.75초,
비트 그리드 스냅)"로 교체 — 음악 싱크가 목적이며, 위 표의 자연 케이던스(손별 0.73~1.5초)와도 정합.
2026-07-12 난이도 개편으로 easy 4/3/2·normal 3/2.5/2비트가 됨 (구 easy 폐지, 구 normal→easy).*
